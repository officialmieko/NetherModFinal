pluginManagement {
    repositories {
        maven { url 'https://maven.fabricmc.net/' } // Fabric's Maven
        gradlePluginPortal() // Default Gradle plugins portal
        mavenCentral()
    }
}

rootProject.name = 'NetherMod_Optimize'